// test-paddle-order.js — deprecated
// This repository no longer uses WebSocket relay tests. The TalkJS + HTTP
// join/leave flow replaced the old websocket-based multiplayer transport.
// If you need to simulate behavior for manual testing, use the HTTP /join
// endpoint or send messages via the TalkJS Data API.

console.log('[test-paddle-order] deprecated — no-op');
